version: v1
managed:
  enabled: true
  optimize_for: CODE_SIZE
  go_package_prefix:
    default: github.com/dotdak/protoc-gen-gorm-dao/gorm
plugins:
  - name: go
    out: gorm
    opt: paths=source_relative
